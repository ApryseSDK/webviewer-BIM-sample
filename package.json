{
  "name": "react-latest-webviewer-bim",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@pdftron/webviewer": "8.10",
    "@pdftron/webviewer-bim-client": "file:./WV3D-MVP/package",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "child_process": "^1.0.2",
    "fs-extra": "^10.1.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "prestart": "rm -rf node_modules/.cache/",
    "start": "node prepare-host.js && PORT=3020 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "dep": "npm run build && HTTP_PROXY=http://127.0.0.1:7890 firebase deploy"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}